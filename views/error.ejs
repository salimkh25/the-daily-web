<%- include('partials/header', { title: (locals.status || 'Error') + ' — The Daily Web' }) %>

<section class="error-page">
  <span class="section-flag">Error</span>
  <h1 class="error-page__code"><%= locals.status || 'Error' %></h1>
  <p class="error-page__message"><%= locals.message || 'Something went wrong.' %></p>
  <a class="btn btn--primary" href="/">Back to the front page</a>
</section>

<%- include('partials/footer') %>
